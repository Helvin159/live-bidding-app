version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bidding-app-container
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
  mongo-data:
    image: mongo:latest
    container_name: mongo
    ports:
      - '27017:27017'
    environment:
      - MONGO_INITDB_ROOT_USERNAME=helvin159@gmail.com
      - MONGO_INITDB_ROOT_PASSWORD=DoRa0411!@!@
      - MONGODB_URI=mongodb+srv://<helvin159@gmail.com>:<DoRa0411!@!@>@cluster22271.zagr6.mongodb.net/?retryWrites=true&w=majority&appName=Cluster22271
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:

# const uri = "mongodb+srv://<helvin159@gmail.com>:<DoRa0411!@!@>@cluster22271.zagr6.mongodb.net/?retryWrites=true&w=majority&appName=Cluster22271";
